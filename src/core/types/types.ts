export type TAccordionData = {
  id: number;
  img: string;
  label: string;
  text: string;
  vectorI: string;
};

export type TCommentsData = {
  id: number;
  img: string;
  text: string;
  name: string;
};

export type TIntroItemRow = {
  id: number;
  text?: string;
  img: string;
};
